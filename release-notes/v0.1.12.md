# Release v0.1.12

Release highlights:

- Update README with clearer beginner-friendly quickstart and CLI docs.
- Add OpenAPI integration improvements and Pydantic query-parameter schema
  generation (`validate_query` and component schemas).
- Add examples and tests for query-model OpenAPI generation (`examples/query_app.py`,
  `tests/test_openapi_query.py`).
- Small middleware and docs improvements, plus internal housekeeping.

For full developer notes and a longer changelog, see `CHANGELOG.md`.
